# No real SMTP call happens in tests: IEmailService is replaced with a
# @MockitoBean in EmailControllerIT, and EmailServiceImplTest mocks
# JavaMailSender/TemplateEngine directly. These dummy values only exist so
# EmailConfig's JavaMailSenderImpl bean can be created at context startup.
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=test@example.com
spring.mail.password=test-password

jwt.secret=test-secret-just-for-tests-0123456789

app.cors.allowed-origins=http://localhost:4200
